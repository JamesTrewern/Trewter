.row
  .col
    %h.elementTitle Comments
-if comment_depth > 0
  -x = 0
  -post.comments.each do |comment|
    = render 'comments/show', comment: comment
    -x += 1
    -break if x == comment_depth
-else
  -post.comments.each do |comment|
    = render 'comments/show', comment: comment
.row#addCommentRow
  .col.form
    = render 'comments/form', comment: Comment.new,  post: post
